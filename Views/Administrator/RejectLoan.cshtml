@model LoanManagementSystem.Models.Loan

@{
    ViewData["Title"] = "RejectLoan";
}

<h1>RejectLoan</h1>

<h4>Loan</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="RejectLoan">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="RejectionReason" class="control-label"></label>
                <textarea asp-for="RejectionReason" class="form-control"></textarea>
                <span asp-validation-for="RejectionReason" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="hidden" asp-for="UserId" class="form-control" />
            </div>
            <div class="form-group">
                <input type="submit" value="Submit" class="btn btn-success" />
                <a asp-action="Loan" class="btn btn-danger">Cancel</a>

            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
